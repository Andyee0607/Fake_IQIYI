package com.ruoyi.web.controller.adapter;

public interface view {
    /**
     * 观看历史方法request
     * @param userId
     * @param data
     * @param type
     */
    void request(Long userId, String data, String type);
}
